#include <stdio.h>

char	*ft_strstr(char *str, char *to_find);

int main(void)
{

	return (0);
}

char	*ft_strstr(char *str, char *to_find)
{
	if (to_find == " ")
		return (str);
	return (to_find);
}
