#include <stdlib.h>
#include <stdio.h>

char    *ft_strjoin(int size, char **strs, char *sep)
{
    char    *final_str;

    if (size == 0)
    {
        *strs = NULL;
        free(strs);
    }
    return (final_str);
}

int     main(void)
{
    return (0);
}
