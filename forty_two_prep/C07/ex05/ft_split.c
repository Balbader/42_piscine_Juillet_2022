#include <stdio.h>
#include <stdlib.h>

//  1. create an 2d array that will take in every segment of the string the is
//     preceded and followed by any character within the *split string.

char **ft_split(char *str, char *charset)
{
	char **tab;
	return (tab);
}

int main(void)
{
	char *str = "Hello my name is balou";
	char *split = "aeiou";
	return (0);
}
